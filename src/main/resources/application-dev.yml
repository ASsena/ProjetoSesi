spring:
  application:
    name: sesi
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/projeto
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
    open-in-view: false

